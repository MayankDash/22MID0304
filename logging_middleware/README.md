# Logging Middleware

Reusable Log() function for the evaluation platform.

Usage

```js
const { Log } = require("./logging_middleware");

Log("backend", "info", "route", "GET /api/v1/notifications");
```
